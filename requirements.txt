# Core — работают на всех Python 3.11+
# Используем версии с готовыми wheel-файлами (не требуют Rust)
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.8.0
python-multipart>=0.0.9

# HTTP client
httpx>=0.27.0

# Email
aiosmtplib>=3.0.0

# Environment
python-dotenv>=1.0.0
